# --- UI / framework ---
chainlit>=2.0,<3.0

# --- LangGraph + LangChain ---
langgraph>=0.2
langgraph-checkpoint-postgres>=2.0
langchain>=0.3
langchain-core>=0.3
langchain-community>=0.3
langchain-openai>=0.2
langchain-text-splitters>=0.3
langsmith>=0.1

# --- Postgres drivers ---
psycopg[binary,pool]>=3.2
asyncpg>=0.29
sqlalchemy[asyncio]>=2.0

# --- Chainlit S3-compatible storage (Backblaze B2) ---
boto3>=1.34

# --- RAG (vector store + loaders) ---
chromadb>=0.5
pypdf>=4.0
docx2txt>=0.8

# --- Tools ---
tavily-python>=0.5

# --- Misc ---
python-dotenv>=1.0
